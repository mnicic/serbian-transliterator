import { TransliteratorPipe } from './transliterator.pipe';

describe('TranslitPipe', () => {
  it('create an instance', () => {
    const pipe = new TransliteratorPipe();
    expect(pipe).toBeTruthy();
  });
});
